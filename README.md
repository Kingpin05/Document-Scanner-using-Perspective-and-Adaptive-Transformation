# Document-Scanner-using-Perspective-and-Adaptive-Transformation
A Python script using OpenCV and NumPy that mimics a document scanning application. It allows users to interactively select the four corners of a document in an image to straighten it and apply a clean, high-contrast filter.
